import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "All Services",
  description: "Browse all social media engagement services for TikTok, YouTube, Instagram, Facebook, X (Twitter), and Telegram.",
};

export { default } from "./ServicesClient";
